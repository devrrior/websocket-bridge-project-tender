enum QueueName {
	newProject = "projectTender.project.new",
	newProjectList = "projectTender.projectList.new",
	newUser = "projectTender.user.new",
	currentUser = "projectTender.user.new",
}

export default QueueName;
